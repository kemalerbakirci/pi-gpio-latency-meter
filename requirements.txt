# Core dependencies
numpy>=1.20.0
matplotlib>=3.5.0

# GPIO libraries (Linux only)
gpiod; platform_system == "Linux"

# Optional: pigpio for DMA timestamps
# pigpio; platform_system == "Linux"

# Development dependencies (optional)
# pytest>=6.0
# black>=22.0
# isort>=5.0
# flake8>=4.0
# pre-commit>=2.0
